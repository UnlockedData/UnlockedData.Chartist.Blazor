name: ci-build

on:
  push:
    branches:
      - main
      - 'rel/v*'
    paths-ignore:
      - README.md
  pull_request:
    branches:
      - main
    paths-ignore:
      - README.md

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0 # avoid shallow clone so nbgv can do its work.
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: |
          9.0.x
          10.0.x
    - uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: nuget-
    - name: nbgv set version
      uses: dotnet/nbgv@v0.4
      with:
        setAllVars: true
    - run: echo "NBGV_SemVer2 $NBGV_SemVer2"
    - name: Build
      run: dotnet build UnlockedData.Chartist.Blazor.sln --configuration Release
    - name: Unit Tests
      run: dotnet test UnlockedData.Chartist.Blazor.sln --no-build -c Release --verbosity normal
    - name: Pack
      run: dotnet pack UnlockedData.Chartist.Blazor.sln --no-build -c Release --output "${{ github.workspace }}/package"
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nuget
        path: ${{ github.workspace }}/package
    - name: Push to NuGet
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/rel/v')
      run: dotnet nuget push "${{ github.workspace }}/package/*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
